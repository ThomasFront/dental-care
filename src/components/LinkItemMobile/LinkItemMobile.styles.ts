import styled from "styled-components";

export const LinkItem = styled.a`
  margin: 6px 0;
  padding: 16px;
  cursor: pointer;
  transition: color 0.2s;

  &:hover {
      color: #02adbe;
    }
`